# backend-node

RealWorld API implemented on raw Node + Express (Track 1 of the learning plan — see `../LEARNING_PLAN.md`).

To install dependencies:

```bash
bun install
```

Copy `.env.example` to `.env` and adjust if needed (must match `../docker-compose.yml`).

To run in dev (watch mode):

```bash
bun run dev
```
